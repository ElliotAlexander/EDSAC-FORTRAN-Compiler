C AREA OF A TRIANGLE - HERON'S FORMULA
C INPUT - CARD READER UNIT 5, INTEGER INPUT
C OUTPUT -
C INTEGER VARIABLES START WITH I,J,K,L,M OR N
      SUBROUTINE DASHP(X,Y,D)
      CALL PLOT(X,Y,2)
      RETURN
      READ(5,501) IA,IB,IC
      GO TO 55, (100, 44, 120), 55
      GO TO 44
      GOTO 55, (10, 10, 023123, 55)
      DO 10000000 HELLOWORLD=55,19
      DO 10 NEWVAR=55,OLDVAR
      DO 10 NEWVAR=55,OLDVAR,NOT434
  501 FORMAT(3I5)
      DIMENSION ALPHA(100), GAMMA(25), VECTOR(12)
      IF(IA.EQ.0 .OR. IB.EQ.0 .OR. IC.EQ.0) 1
      S = (IA + IB + IC) / 2.0
      AREA = SQRT( S * (S - IA) * (S - IB) * (S - IC) )
      ASSIGN NEWVAR TO 55
      WRITE(6,601) IA,IB,IC,AREA
  601 FORMAT(4H A= ,I5,5H  B= ,I5,5H  C= ,I5,8H  AREA= ,F10.2,
     $13H SQUARE UNITS)
      STOP
      END (HEL2LD)