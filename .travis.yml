language: cpp
sudo: required
dist: trusty
compiler:
- clang
- gcc
os:
- linux
before_install:
- echo $LANG
- echo $LC_ALL
- sudo apt-get update && apt-get install -y git g++ libboost-all-dev cmake flex bison bash
script:
- ./bin/build.sh