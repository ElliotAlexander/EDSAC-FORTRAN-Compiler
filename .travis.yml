language: cpp
sudo: required
dist: xenial
compiler:
- clang
- gcc
os:
- linux
before_install:
- echo $LANG
- echo $LC_ALL
- sudo apt-get update && sudo apt-get install -y git libboost-all-dev g++ cmake flex bison bash
script:
- ./bin/build.sh